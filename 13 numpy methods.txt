- 최댓값, 최솟값
max 메소드와 min 메소드를 사용하면 numpy array의 최댓값과 최솟값을 구할 수 있습니다.

array1 = np.array([14, 6, 13, 21, 23, 31, 9, 5])

print(array1.max())
print(array1.min())

#31
#5
--------------------------------------------------------------------------------------------------------------
- 평균값
mean 메소드를 사용하면 numpy array의 평균값을 구할 수 있습니다.

array1 = np.array([14, 6, 13, 21, 23, 31, 9, 5])

print(array1.mean())

#15.25
--------------------------------------------------------------------------------------------------------------
- 중앙값
median 메소드를 사용하면 중간값을 구할 수 있습니다.. 특이하게 median은 numpy array의 메소드가 아니라 numpy의 메소드입니다.

array1 = np.array([8, 12, 9, 15, 16])
array2 = np.array([14, 6, 13, 21, 23, 31, 9, 5])

print(np.median(array1))
print(np.median(array2))

#12.0
#13.5  # 짝수 개는 두 중앙값의 평균을 산출
--------------------------------------------------------------------------------------------------------------
- 표준 편차, 분산

array1 = np.array([14, 6, 13, 21, 23, 31, 9, 5])

print(array1.std()) # 표준 편차
print(array1.var()) # 분산

#8.496322733983215
#72.1875
